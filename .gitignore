# Defold build artifacts
/build
/.internal

# bob.jar (downloaded by CI, don't commit the 200MB jar)
/bob.jar
release.keystore
keystore_pass.txt
key_pass.txt

# OS / editor junk
.DS_Store
Thumbs.db
*.swp
.vscode/
