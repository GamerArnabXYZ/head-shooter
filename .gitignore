# Godot 3.x — auto-generated import cache, safe to ignore, regenerated on open
.import/

# NOTE: export_presets.cfg is intentionally NOT ignored — GitHub Actions
# needs it to know the Android + Web export presets. It contains no
# secrets (keystore fields are empty; CI uses the image's debug keystore).

# Local build output (CI writes here too, but we don't want to commit it)
build/

# OS / editor junk
.DS_Store
*.tmp
*.log
